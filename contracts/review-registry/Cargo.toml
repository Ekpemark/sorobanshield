[package]
name = "sorobanshield-review-registry"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "22.0.0"

# Build with: cargo build --target wasm32v1-none --release
